<h1><%= @user.email %></h1>

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">

        <% @user.friends.each do |friend| %>
           <div><%= friend.name %> - <%= friend.age %>
          <%= friend.description %>
          <%= friend.city %></div>
        <% end %>

    </div>
  </div>
</div>
